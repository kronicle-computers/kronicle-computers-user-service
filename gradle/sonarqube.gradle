apply plugin: 'org.sonarqube'

sonarqube {
    properties {
        property "sonar.DISABLEprojectKey", "kronicle-computers_user-service"
        property "sonar.DISABLEorganization", "kronicle-computers"
        property 'sonar.host.url', sonarQubeHostUrl

        property 'sonar.DISABLEprojectName', 'user-service'
        property 'sonar.projectVersion', version
        property 'sonar.links.homepage', websiteUrl

        property 'sonar.sourceEncoding', 'UTF-8'

        if (project.name == "app") {
            property 'sonar.cobertura.reportPath', 'coverage/cobertura-coverage.xml'
        }
    }
}
